<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ApiXamarin.Views.PlayerInfoView"
             Title="Players Info">

    <ListView ItemsSource="{Binding PlayersInfos}">
        <ListView.ItemTemplate >
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="Hola"/>
                        <Label Text="{Binding PlayersInfos.Name}"/>
                        <Label Text="{Binding FirstName}"/>
                        <Label Text="{Binding LastName}" />
                        <Label Text="{Binding DateOfBirth}"/>
                        <Label Text="{Binding CountryOfBirth}"/>
                        <Label Text="{Binding Nationality}" />
                        <Label Text="{Binding Position}" />
                        <Label Text="{Binding LastUpdated}" />

                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>