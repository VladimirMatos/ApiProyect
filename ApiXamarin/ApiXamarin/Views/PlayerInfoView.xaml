<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ApiXamarin.Views.PlayerInfoView"
             Title="Players Info">
    <ListView ItemsSource="{Binding PlayersInfos}">
        <ListView.ItemTemplate >
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding Info.ID}"/>
                <Label Text="{Binding Info.Name}"/>
                <Label Text="{Binding Info.FirstName}"/>
                <Label Text="{Binding Info.LastName}" />
                <Label Text="{Binding Info.DateOfBirth}"/>
                <Label Text="{Binding Info.CountryOfBirth}"/>
                <Label Text="{Binding Info.Nationality}" />
                <Label Text="{Binding Info.Position}" />
                <Label Text="{Binding Info.LastUpdated}" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>